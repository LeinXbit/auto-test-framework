# 本地 gin-vue-admin 真实业务环境
base_url: "http://127.0.0.1:8888"

# 管理员账号（用于 fixture 自动登录）
admin:
  username: "admin"
  password: "123456"

# 验证码识别配置
captcha:
  # GVA 强制开启验证码，使用 OCR 自动识别
  solver: "ddddocr"
  # 验证码长度不匹配时重试，识别失败最大重试次数
  max_retry: 5
  # 期望的验证码长度（GVA 默认 6 位）
  expected_length: 6

database:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "GJH123gjh+"
  database: "gva"

timeout: 10
